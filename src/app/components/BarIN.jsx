import React from "react";

function BarIN({ bgColor,height  }) {
  return (
    <div
      style={{ backgroundColor: bgColor, height: height, width: "2px" }}
    ></div>
  );
}

export default BarIN;
